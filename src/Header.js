
const Header = ({title}) => {
  return (
    <header className="header">{title}</header>
  )
}

export default Header






// const Header = ({title}) => {
//     return (
//         <header className="header">{title}</header>
//     )
// }
// export default Header;